# Database Package

This package is the reference home for database conventions, migrations, seeds, backup notes, and PostgreSQL operational checks.

Products may replace the placeholder migration runner with a real migration tool, but must preserve:

- migration history
- backup and restore instructions
- upgrade and rollback notes
- offline execution compatibility

